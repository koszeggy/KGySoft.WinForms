Legend:
! Breaking change
* Compatible change
- Bugfix
+ New feature
===============================================================================

~~~~~~~~~
! v5.0.0:
~~~~~~~~~

+ Assembly changes:
===================
+ Targeting also .NET Framework 4.7, .NET 5.0 and 7.0.

! KGySoft.Libraries.Language namespace
======================================
! Every type in this namespace has been marked as obsolete. This is a legacy namespace from the old
  KGySoft.Language.dll dependency. The old translation mechanism doesn't translate anything anymore, just removes
  the possible distinction postfixes if present. To localize your application in a similar way with auto-generated
  resources, use the DynamicResourceManager class from the KGySoft.CoreLibraries package. For more details see:
  DynamicResourceManager class: https://docs.kgysoft.net/corelibraries/html/T_KGySoft_Resources_DynamicResourceManager.htm
  Application example: https://github.com/koszeggy/KGySoft.Drawing.Tools#localization

! Language class: It has been made obsolete. See the note above.
! ICustomTranslated interface: It has been made obsolete. See the note above.

! KGySoft.WinForms namespace:
=============================
! DataGridViewTextAndImageColumn class: Removed, it was already obsolete.
! DataGridViewTextAndImageCell class: Removed, it was already obsolete.
+ New ScaleHelper class
+ New WinFormsModule class
+ New EventHandlerExtensions class
+ New AsyncTaskBase class: Migrated from the KGySoft.Drawing.ImagingTools project.
+ New LocalizationHelper class: can be used instead of the obsoleted Language class to localize WinForms
  applications with auto-generated resources.
+ New LocalizationContext class
+ New DynamicStringLocalization enum
+ New LocalizationRequestedEventArgs class
+ New ICustomLocalizable interface
+ New IToolTipTargetProvider interface
+ New OSHelper class

+ KGySoft.ComponentModel namespace:
===================================
+ WinFormsCommandBindingsCollection class:
  + New SupportCrossThreadStateUpdates property: to allow updating command binding states (e.g. Enabled) from
    background threads.
+ The ExpandablePropertiesConverter class has been extracted from the RecursivelyEditableTypeDescriptor class
  and has been made public.

* KGySoft.WinForms.Components namespace:
========================================
* TaskDialog class:
  + Supporting high DPI and per-monitor DPI awareness.
  + Ctrl+C copies the textual content of the task dialog to the clipboard even in compatibility mode.
  - Fixing widths
  - Native mode: fixing some rare exception cases during Dispose
  - Native mode: when owner was not specified, the caller window remained enabled
  - Compatibility mode: opening without an owner was still modal, which is not how the native TaskDialog works.
  * Compatibility mode: improving expando button image quality, showing ? button if HelpRequested event is assigned,
    adjusting sizes of custom button icons.
+ TaskDialogOptions enum:
  + New ForceShowSysMenu option
  + New ForceShowInTaskbar option

! KGySoft.WinForms.Controls namespace:
======================================
! AdvancedButton class:
  ! Changing Standard classic (when there are no visual styles) and Popup FlatStyle rendering with images: just like
    the text, the image is also shifted when the button is pressed. This is how the native rendering also works.
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark (when FlatStyle is Popup or Flat,
    or visual styles are not available).
  ! BackColor/ForeColor: when the control is not Enabled, they get/set the disabled colors. The Changed events are
    raised also when the Enabled state changes and the enabled/disabled colors are different.
  + New EnabledBackColor/EnabledForeColor properties: for symmetry with the existing DisabledBackColor and
    DisabledForeColor properties.
  + Making the overridden ForeColor/BackColor properties visible in the designer.
  + Supporting High DPI
  + New TextRenderingQuality property: brings back the functionality of the removed RenderingQuality property,
    but only for the text.
  + Focus rectangle width is OS and DPI dependent.
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
  - Fixing appearance in high contrast mode with visual styles (Windows 10 and above). This issue presents in the
    original Button class as well. Classified as a fix rather than a breaking change as the issue is visual only.
! AdvancedCheckBox class:
  ! Rendering change with Button appearance - see AdvancedButton.
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark (when FlatStyle is Popup or Flat,
    or visual styles are not available).
  ! BackColor/ForeColor: when the control is not Enabled, they get/set the disabled colors. The Changed events are
    raised also when the Enabled state changes and the enabled/disabled colors are different.
  + New EnabledBackColor/EnabledForeColor properties: for symmetry with the existing DisabledBackColor and
    DisabledForeColor properties.
  + Making the overridden ForeColor/BackColor properties visible in the designer.
  + Supporting High DPI
  + New TextRenderingQuality property: brings back the functionality of the removed RenderingQuality property,
    but only for the text.
  + New VisualsRenderingQuality property: affects the quality of the check box when no proper size is available by
    the visual style renderer on the current DPI.
  + Focus rectangle width is OS and DPI dependent.
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
! AdvancedRadioButton class:
  ! Rendering change with Button appearance - see AdvancedButton.
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark (when FlatStyle is Popup or Flat,
    or visual styles are not available).
  ! BackColor/ForeColor: when the control is not Enabled, they get/set the disabled colors. The Changed events are
    raised also when the Enabled state changes and the enabled/disabled colors are different.
  + New EnabledBackColor/EnabledForeColor properties: for symmetry with the existing DisabledBackColor and
    DisabledForeColor properties.
  + Making the overridden ForeColor/BackColor properties visible in the designer.
  + Supporting High DPI
  + New TextRenderingQuality property: brings back the functionality of the removed RenderingQuality property,
    but only for the text.
  + New VisualsRenderingQuality property: affects the quality of the check area when no proper size is available by
    the visual style renderer on the current DPI.
  + Focus rectangle width is OS and DPI dependent.
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
! AdvancedLabel class:
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark
  ! BackColor/ForeColor: when the control is not Enabled, they get/set the disabled colors. The Changed events are
    raised also when the Enabled state changes and the enabled/disabled colors are different.
  + New EnabledBackColor/EnabledForeColor properties: for symmetry with the existing DisabledBackColor and
    DisabledForeColor properties.
  + Making the overridden ForeColor/BackColor properties visible in the designer.
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
  - Not serializing LinkArea property in the designer if there are no links defined or ResolveHyperlinks is not set.
! AdvancedProgressBar class:
  ! In high contrast mode rendering with the classic style only, no matter what the Style property is.
! CommandLinkButton class:
  ! Different reported preferred size when FlatStyle is not System.
  ! When available, using the visual style renderer for the arrow glyph in non-System FlatStyles.
  ! The themed colors are taken from the visual style renderer.
  ! Allowing custom default arrow glyph color (Windows 10 and above)
  ! Not using visual style colors in Popup/Flat mode.
  ! The BackColor/ForeColor properties really reflect the actual colors of the control in the current Enabled state.
  + New EnabledBackColor/EnabledForeColor properties: for symmetry with the existing DisabledBackColor and
    DisabledForeColor properties.
  + Making the overridden ForeColor/BackColor properties visible in the designer.
  + Supporting High DPI, adding more resolutions for the arrow glyph when visual styles are not available.
  + New TextRenderingQuality property: brings back the functionality of the removed RenderingQuality property,
    but only for the text.
  + New VisualsRenderingQuality property: affects the quality of the arrow glyph on some specific DPI settings,
    or when no visual style renderer is available.
  + When FadingOptions.Text is enabled, using buffered animation also for Description changes
  + When FadingOptions.Color is enabled, using buffered animation also for the description color changes.
  + New AutoScaleFont property to allow automatic scaling of the fonts when the DPI changes. Without this the
    DescriptionFont is never scaled, whereas Font would be scaled on the newer platforms only.
  - Improving high contrast appearance
  - Fixing colors and fonts when changing back and forth between high contrast mode and visual style themes
  - Fixing top image alignment
! AdvancedComboBox class:
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark
  + Making overridden ForeColor/BackColor properties visible in the designer.
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
  - Fixing AutoCompleteMode if it was already set in the designer.
  - Fixing ReadOnly mode in Simple style: if the list box area was visible, it was possible to change the selection.
  - Fixing ReadOnly mode in DropDownList style: up to now it was faked by disabling the control.
! AdvancedTextBox class:
  ! Default DisabledForeColor is SystemColors.GrayText rather than ControlDarkDark
  ! In Multiline mode AcceptsReturn was ignored and the Return key was forcibly handled to insert a new line. Now
    AcceptsReturn and AcceptsTab are respected when ReadOnly is false; otherwise, they are ignored, and the Return
    and Tab keys are allowed to be handled by the parent form as if the properties were false.
  + Making overridden ForeColor/BackColor properties visible in the designer.
  + Making Ctrl+A work even if auto appending is enabled
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
! AdvancedDateTimePicker:
  ! Custom rendering even in Enabled mode when the control is not in focus.
  + New EnabledBackColor/EnabledForeColor properties: they allow custom enabled colors when the control is not in
    focus. When the control is in focus, only custom background color is supported with no visual styles available.
  + Making overridden ForeColor/BackColor properties visible in the designer.
  + Improving disabled rendering
  + New AutoScaleFont property to allow automatic scaling of the font when the DPI changes even on platforms where
    this is not supported by default.
  + New CheckBoxRenderingQuality property: allows to set the quality of the check box rendering when no proper size
    is available by the visual style renderer on the current DPI.
+ New BaseUserControl class: Migrated from the KGySoft.Drawing.ImagingTools project.
+ New BaseControl class: Migrated from the KGySoft.Drawing.ImagingTools project.
+ New OkCancelButtons class: Migrated from the KGySoft.Drawing.ImagingTools project.
+ New ImageViewer class: Migrated from the KGySoft.Drawing.ImagingTools project.
+ New ImageViewerOptimizationOptions enum

! KGySoft.WinForms.Forms namespace:
===================================
! BaseForm class:
  ! The BaseToolTip field has been concealed by the EditorBrowsableState.Never attribute. Actually not a breaking
    change as the code referencing the field is still working, but the field is no longer visible in the IDE. Use
    the new ToolTip property instead.
  ! The default timings of BaseToolTip field (ToolTip property) have been changed.
  + New ToolTip property
  + Event subscriptions to self events are automatically removed when the form is disposed.
  + New CommandBindings property
  + New IsDesignMode property
  + New InvokeOnUIThread method: a safer and more convenient way to invoke a delegate on the UI thread.
  + New DeviceScale property
  + New DeviceScaleChanged event: raised when the DPI changes and the application is per-monitor DPI aware.
  + New IsLoaded property
  + New DynamicStringLocalization property and DynamicStringLocalizationChanged event
  + New ApplyResources and ApplyStringResources methods
  + Scaling the small icon of the form properly when the DPI changes.
! DialogBaseForm class:
  ! The protected btnOk, btnCancel and pnlButtons fields have been replaced by read-only properties. The original
    members are still available, but they have been concealed by the EditorBrowsableState.Never attribute.
  ! The type of the pnlButtons member has been changed to OkCancelButtons (used to be TableLayoutPanel).
  + New ShowOKButton, ShowCancelButton and ShowApplyButton properties
+ New DeviceScaleChangedEventArgs class


~~~~~~~~~
! v4.0.0:
~~~~~~~~~

! Assembly changes:
===================
! The KGySoft.Controls.dll assembly has been renamed to KGySoft.WinForms.dll
+ Targeting also .NET Core 3.0.

+ KGySoft.ComponentModel namespace:
===================================
+ New WinFormsCommandBindingsCollection class

! KGySoft.Controls namespace:
=============================
! The namespace has been removed and its content have been moved to:
  ! KGySoft.ComponentModel
  ! KGySoft.WinForms
  ! KGySoft.WinForms.Components
  ! KGySoft.WinForms.Controls
  ! KGySoft.WinForms.Forms
! Completely removed types:
  ! ComboUtils (it was obsolete already in v3.5.2.0)
  ! Unicode
  ! WaitForm
  ! ...

! KGySoft.Controls.Interop namespace:
=====================================
! The namespace has been removed and its AdvancedWinformsHost WPF control does not belong to this assembly anymore.

+ KGySoft.WinForms namespace:
=============================
+ BaseForm class:
  + New CommandBindings property
  ...

- KGySoft.WinForms.Components namespace:
========================================
- TaskDialog class:
  - Changing the way of creating the activation context for theming to be compatible with .NET Core